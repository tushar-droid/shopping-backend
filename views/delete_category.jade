extends layout 

block content 

    h1= title 

    h3= category.name 

    if products.length 
        p You need to delete all the associated products in the category 
        ul 
            for product in products 
                h5 
                    a(href=product.url) #{product.name} 
    else 
        p Are you sure you want to delete this category?

        form(method='POST')
            input(type='hidden', name='category_id', value= category._id )
            button(type='submit') Delete 
        button 
            a(href= category.url) Cancel